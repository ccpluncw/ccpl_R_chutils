% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ch.quantileStats.r
\name{ch.quantileStats}
\alias{ch.quantileStats}
\title{Compute sample quantiles with variance estimates}
\usage{
ch.quantileStats(
  x,
  quantiles = 0.5,
  n_boot = 1000,
  method = c("bootstrap", "mj"),
  na.rm = TRUE
)
}
\arguments{
\item{x}{A numeric vector of observations (e.g. per-cell RT residuals).}

\item{quantiles}{A numeric vector of probabilities in (0,1). Default 0.5.}

\item{n_boot}{Number of bootstrap resamples (bootstrap method only). Default 1000.}

\item{method}{Variance estimator: "bootstrap" (default) or "mj" (Maritz-Jarrett).}

\item{na.rm}{Logical; drop NAs before computing. Default TRUE.}
}
\value{
A named list keyed by paste0("Q", round(q*100)). Each element is a
  list with `value` (the sample quantile) and `var` (its variance estimate).
  `var` is NA when fewer than 2 non-NA observations are available.
}
\description{
For each requested quantile, returns the sample quantile and an estimate of
its sampling variance, computed either by bootstrap (default) or the
Maritz-Jarrett method. Used by the RRW2 multi-quantile fitting pipeline to
build per-cell RT quantile targets and the variances that weight them.
}
\examples{
ch.quantileStats(rnorm(500), quantiles = c(0.1, 0.5, 0.9))
}
\keyword{bootstrap}
\keyword{quantile}
\keyword{variance}
